/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config, options) => {
    //     config.module.rules.push({
    //         test: /\.ya?ml$/,
    //         use: "js-yaml-loader",
    //     })
    //     return config
    // },
}

module.exports = nextConfig
